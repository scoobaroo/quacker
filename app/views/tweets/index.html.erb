<h1>Index of Tweets</h1>
<% @tweets.each do |t| %>
<%= link_to t.title, tweet_path(t) %>
<%= t.body %>
<%= t.longitude %>
<%= t.latitude %>
<br>
<% end %>
<%= link_to "Post New Tweet", new_tweet_path %>
